// Gutted to a placeholder (2026-09-02) — section content is being rebuilt
// from scratch; kept as just its own title so the homepage stays navigable
// (id + section-indicator wiring) while that happens.
export function PhilosophyStatement() {
  return (
    <section id="philosophy" className="px-6 py-32 max-w-[1600px] mx-auto scroll-mt-24">
      <p className="text-xs uppercase tracking-widest text-kov-steel">Philosophie</p>
    </section>
  );
}
