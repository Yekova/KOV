// Gutted to a placeholder (2026-09-02) — section content is being rebuilt
// from scratch; kept as just its own title so the homepage stays navigable
// while that happens. Wasn't tracked by KovSectionIndicator's SECTIONS list
// before this either (id is new here, purely for wayfinding — safe to add).
export function WorkSpotlight() {
  return (
    <section id="spotlight" className="px-6 py-32 max-w-[1600px] mx-auto scroll-mt-24">
      <p className="text-xs uppercase tracking-widest text-kov-steel">Spotlight</p>
    </section>
  );
}
